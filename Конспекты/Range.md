# Range

Range это форма цикла `for` для итерации по slice или map.

Для каждой итерации возвращается 2 значения - индекс и копия элемента по указанному индексу:
```
for i, v := range pow {
		fmt.Printf("2**%d = %d\n", i, v)
	}
```